import { SchedulerCalendar } from './calendar'
import { SchedulerSelect } from './select'

export const Scheduler = () => {
  
  return (
    <>
      <SchedulerSelect />
      <SchedulerCalendar />
    </>
  )
}
